import { ImagenErrorDirective } from './imagen-error.directive';

describe('ImagenErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new ImagenErrorDirective();
    expect(directive).toBeTruthy();
  });
});
