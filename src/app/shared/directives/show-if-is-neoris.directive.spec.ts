import { ShowIfIsNeorisDirective } from './show-if-is-neoris.directive';

describe('ShowIfIsNeorisDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowIfIsNeorisDirective();
    expect(directive).toBeTruthy();
  });
});
